# JAX core
jax==0.7.2
jaxlib==0.7.2
ml_dtypes==0.5.4

# JAX ecosystem
chex==0.1.92
diffrax==0.7.2
equinox==0.13.8
jaxtyping==0.2.38
lineax==0.1.0
optimistix==0.1.0
optax==0.2.8
wadler-lindig==0.1.7

# Data & utilities
h5py==3.16.0
numpy==2.5.0
opt-einsum==3.4.0
orbax==0.1.9
pandas==3.0.3
PyYAML==6.0.3
ray==2.55.1

# GPU (Linux only) — replace the jax/jaxlib lines above with:
#   pip install -U "jax[cuda13]"       # CUDA 13 (recommended)
#   pip install -U "jax[cuda12]"       # CUDA 12 (still supported, future drop planned)
